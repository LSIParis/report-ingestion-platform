# Canal d'alerte Desk365 — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Une alerte critique ouvre un ticket Desk365 (au lieu d'un webhook générique), et sa fermeture annote ce ticket — sans que le technicien ait à surveiller un écran de plus.

**Architecture:** Le canal d'alerte devient sélectionnable (`ALERT_CHANNEL`). On extrait le webhook existant dans un paquet `channels/`, on ajoute un canal `desk365.py` qui implémente le même contrat, et une colonne `alert.external_ref` mémorise le numéro de ticket pour l'annoter à la fermeture. L'idempotence, les retries et le filet de rattrapage — tout ce qui a coûté cher à stabiliser dans le système d'alertes — vivent **au-dessus** du canal et ne changent pas.

**Tech Stack:** Python 3.12 · FastAPI · SQLAlchemy 2.0 · Alembic · Celery · PostgreSQL 16 (RLS) · `urllib.request` (stdlib, aucune dépendance HTTP nouvelle).

**Spec:** `docs/superpowers/specs/2026-07-15-alerte-desk365-design.md`

## Global Constraints

- **Aucune nouvelle dépendance.** `urllib.request` de la bibliothèque standard, comme le canal webhook et `services/onboarding.py`. Ni `httpx`, ni `requests`.
- **Tout le couplage à Desk365 dans UN fichier** : `app/services/alerting/channels/desk365.py`. Le reste du système d'alertes ne connaît que le contrat `envoyer(event, alert, tenant) -> bool`.
- **Contrat du canal** : `envoyer(event, alert, tenant) -> bool`. Renvoie `True` si quelque chose a été émis, `False` si le canal n'est pas configuré **ou** si l'événement ne doit rien produire. Lève `CanalIndisponible` si le canal est configuré mais l'appel externe échoue (Celery retentera).
- **Non configuré ≠ silencieux** : pas de clé API → `False` + journalisé. On n'avale jamais une alerte sans le dire.
- **L'appel externe ne casse jamais le flux** : `CanalIndisponible` → retry Celery. L'alerte reste ouverte en base ; un canal en panne fait perdre une notification, jamais une alerte.
- **Seules les alertes `severity == "critical"` créent un ticket.** Un avertissement (`tls_failure` en `testing`) reste sur la page Alertes, sans ticket. La sélection porte sur la sévérité, **jamais** sur le `kind`.
- **On annote à la fermeture, on ne clôture JAMAIS automatiquement.**
- Valeurs par défaut exactes (spec §4) : demandeur `alerte_dmarc@lsiparis.tech`, groupe `Support informatique`, priorité `20`, catégorie `Réseau`, sous-catégorie `Déliverabilité emails`, base `https://lsi-maintenance.desk365.io/apis/v3`.
- ⚠️ `structlog` : ne jamais passer un mot-clé nommé `event=` à un appel de log (il percute le premier argument positionnel interne et lève un `TypeError`). Utiliser `alert_event=`.
- Commentaires et textes en français. Messages de commit en français **sans accents**.
- **Commandes** (depuis `infra/`, Git Bash — `MSYS_NO_PATHCONV=1` INDISPENSABLE) :
  - tests : `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api pytest <chemin> -q`
  - migration : `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" migrate alembic upgrade head`
  - lint : `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api ruff check app tests scripts`
- La suite compte **292 tests** au départ et doit rester verte. `tests/test_tenant_isolation.py` est **bloquant**.

## Structure des fichiers

| Fichier | Responsabilité |
|---|---|
| `backend/app/services/alerting/channels/__init__.py` | **Créer.** `get_channel()` : lit `ALERT_CHANNEL`, renvoie le module de canal. |
| `backend/app/services/alerting/channels/base.py` | **Créer.** `CanalIndisponible` — l'exception partagée par tous les canaux. |
| `backend/app/services/alerting/channels/webhook.py` | **Déplacer** depuis `alerting/webhook.py`, à l'identique. `WebhookIndisponible` devient une sous-classe de `CanalIndisponible`. |
| `backend/app/services/alerting/webhook.py` | **Supprimer** (déplacé). |
| `backend/app/services/alerting/channels/desk365.py` | **Créer.** Le canal Desk365 : créer un ticket, annoter à la fermeture. |
| `backend/app/workers/tasks.py` | **Modifier.** Appelle `get_channel()`, attrape `CanalIndisponible`. |
| `backend/app/config.py` | **Modifier.** `alert_channel` + les 7 clés `desk365_*`. |
| `backend/app/db/models.py` | **Modifier.** `Alert.external_ref`. |
| `backend/migrations/versions/0009_alert_external_ref.py` | **Créer.** La colonne. |
| `backend/tests/test_alert_webhook.py` | **Modifier.** Imports mis à jour après le déplacement. |
| `infra/portainer-stack.npm.yml` · `infra/docker-compose.yml` | **Modifier.** Variables `ALERT_CHANNEL` + `DESK365_*` sur le worker (prod) et le worker dev. |

Ordre : 1 (abstraction + déplacement) → 2 (colonne) → 3 (canal Desk365) → 4 (config prod + test réel). La tâche 3 dépend de 1 et 2.

---

### Task 1: L'abstraction `channels/` — et le webhook déplacé à l'identique

**Files:**
- Create: `backend/app/services/alerting/channels/__init__.py`, `backend/app/services/alerting/channels/base.py`, `backend/app/services/alerting/channels/webhook.py`
- Delete: `backend/app/services/alerting/webhook.py`
- Modify: `backend/app/workers/tasks.py`, `backend/app/config.py`, `backend/tests/test_alert_webhook.py`
- Test: `backend/tests/test_alert_channels.py`

**Interfaces:**
- Produces:
  - `app.services.alerting.channels.base.CanalIndisponible` (Exception)
  - `app.services.alerting.channels.get_channel() -> module` (module avec `envoyer(event, alert, tenant) -> bool`)
  - `settings.alert_channel: str` (`"webhook"` | `"desk365"`, défaut `"webhook"`)

Cette tâche est un **refactor à comportement constant** : le canal par défaut reste le webhook, rien ne change fonctionnellement. C'est ce que prouvent les tests existants, qui doivent rester verts après le déplacement.

- [ ] **Step 1: Écrire le test de sélection (échoue)**

Créer `backend/tests/test_alert_channels.py` :

```python
"""La sélection du canal d'alerte. Le systeme d'alertes ne connait qu'un contrat
`envoyer(event, alert, tenant)` ; QUEL canal l'implemente est un choix de configuration.
"""
import pytest

from app.services.alerting import channels
from app.services.alerting.channels import webhook
from app.services.alerting.channels.base import CanalIndisponible


def test_defaut_est_le_webhook(monkeypatch):
    monkeypatch.setattr(channels.settings, "alert_channel", "webhook")
    assert channels.get_channel() is webhook


def test_desk365_selectionnable(monkeypatch):
    from app.services.alerting.channels import desk365
    monkeypatch.setattr(channels.settings, "alert_channel", "desk365")
    assert channels.get_channel() is desk365


def test_canal_inconnu_leve_clairement(monkeypatch):
    """Un ALERT_CHANNEL fautif doit lever bruyamment, pas se rabattre en silence sur un
    canal par defaut : un canal muet choisi par erreur, c'est exactement la panne que ce
    produit combat."""
    monkeypatch.setattr(channels.settings, "alert_channel", "pigeon-voyageur")
    with pytest.raises(ValueError, match="ALERT_CHANNEL"):
        channels.get_channel()


def test_webhook_indisponible_est_un_canal_indisponible():
    """tasks.py attrape `CanalIndisponible` : l'exception du webhook doit en heriter,
    sinon les pannes de webhook ne seraient plus retentees apres le refactor."""
    assert issubclass(webhook.WebhookIndisponible, CanalIndisponible)
```

- [ ] **Step 2: Lancer — échoue**

Run: `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api pytest tests/test_alert_channels.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.services.alerting.channels'`

- [ ] **Step 3: Créer le paquet et l'exception partagée**

Créer `backend/app/services/alerting/channels/base.py` :

```python
"""Le contrat commun à tous les canaux d'alerte.

Un canal expose `envoyer(event, alert, tenant) -> bool` :
 - True  : quelque chose a ete emis ;
 - False : le canal n'est pas configure, OU l'evenement ne doit rien produire ;
 - leve `CanalIndisponible` : le canal est configure mais l'appel externe a echoue
   (Celery retentera).

`workers/tasks.py` n'attrape QUE `CanalIndisponible` : chaque canal fait donc heriter
ses propres exceptions de panne de celle-ci.
"""
from __future__ import annotations


class CanalIndisponible(Exception):
    """Le canal est configuré mais l'appel externe a échoué. Celery retentera."""
```

- [ ] **Step 4: Déplacer le webhook dans le paquet**

Déplacer le fichier :

```bash
git mv backend/app/services/alerting/webhook.py backend/app/services/alerting/channels/webhook.py
```

Dans `backend/app/services/alerting/channels/webhook.py`, ajouter l'import et faire hériter l'exception. Remplacer :

```python
class WebhookIndisponible(Exception):
    """L'URL est configurée mais l'envoi a échoué. Celery retentera."""
```

par :

```python
from app.services.alerting.channels.base import CanalIndisponible


class WebhookIndisponible(CanalIndisponible):
    """L'URL est configurée mais l'envoi a échoué. Celery retentera."""
```

(placer l'`import` avec les autres imports en tête de fichier, pas au milieu du code.)

- [ ] **Step 5: Écrire `get_channel()`**

Créer `backend/app/services/alerting/channels/__init__.py` :

```python
"""Sélection du canal d'alerte selon `ALERT_CHANNEL`.

Ajouter un canal = un fichier ici + une entrée dans `_CANAUX`. Le reste du système
d'alertes n'en sait rien : il appelle `get_channel().envoyer(...)`.
"""
from __future__ import annotations

from app.config import settings
from app.services.alerting.channels import desk365, webhook

_CANAUX = {"webhook": webhook, "desk365": desk365}


def get_channel():
    """Le module de canal correspondant à `settings.alert_channel`.

    Un nom inconnu lève : on ne se rabat JAMAIS en silence sur un canal par défaut — un
    canal muet choisi par erreur reproduirait la panne que ce produit combat.
    """
    canal = _CANAUX.get(settings.alert_channel)
    if canal is None:
        raise ValueError(
            f"ALERT_CHANNEL inconnu : {settings.alert_channel!r} "
            f"(attendu : {', '.join(sorted(_CANAUX))})")
    return canal
```

**Note** : ce fichier importe `desk365`, créé à la tâche 3. Pour que la tâche 1 soit testable seule, créer d'abord un **stub minimal** `backend/app/services/alerting/channels/desk365.py` :

```python
"""Canal Desk365 — implémenté à la tâche 3."""
from __future__ import annotations


def envoyer(event: str, alert, tenant) -> bool:  # remplacé à la tâche 3
    raise NotImplementedError
```

- [ ] **Step 6: Ajouter la configuration du canal**

Dans `backend/app/config.py`, dans le bloc `# --- Alertes ---`, ajouter :

```python
    # Canal d'alerte : "webhook" (défaut, l'existant) ou "desk365" (ticket dans le
    # helpdesk). Le couplage a chaque fournisseur est isolé dans app/services/alerting/
    # channels/<nom>.py.
    alert_channel: str = "webhook"
```

- [ ] **Step 7: Câbler l'appelant**

Dans `backend/app/workers/tasks.py` :

Remplacer l'import `from app.services.alerting import webhook` par :

```python
from app.services.alerting.channels import get_channel
from app.services.alerting.channels.base import CanalIndisponible
```

Dans `notify_alerts`, remplacer la clause `except webhook.WebhookIndisponible as exc:` par :

```python
            except CanalIndisponible as exc:
```

Dans `_notifier_un_evenement`, remplacer l'appel `webhook.envoyer(event, alerte, tenant)` par :

```python
    get_channel().envoyer(event, alerte, tenant)
```

(le commentaire du dessus qui explique la sémantique `False` reste valable — il s'applique à tout canal, pas seulement au webhook.)

- [ ] **Step 8: Mettre à jour les imports des tests existants**

Le webhook a changé de chemin. Chercher toutes les références et les corriger :

```bash
cd infra && MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api sh -c "grep -rn 'alerting.webhook\|alerting import webhook' tests app"
```

Dans `backend/tests/test_alert_webhook.py`, remplacer :

```python
from app.services.alerting import webhook
```

par :

```python
from app.services.alerting.channels import webhook
```

Si un test de `tests/test_alert_sweep.py` remplace (`monkeypatch.setattr`) `webhook.envoyer`, sa cible doit rester le **module** `app.services.alerting.channels.webhook` : `get_channel()` renvoie ce module et appelle `.envoyer` dessus, donc patcher l'attribut du module fonctionne. Vérifier chaque `monkeypatch.setattr(..., "envoyer", ...)` et ajuster le chemin d'import si besoin.

- [ ] **Step 9: Lancer la cible + toute la suite (non-régression)**

Run: `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api pytest tests/test_alert_channels.py tests/test_alert_webhook.py tests/test_alert_sweep.py -q`
Expected: PASS

Puis toute la suite :
Run: `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api pytest -q`
Expected: PASS (292 + 4 nouveaux)

- [ ] **Step 10: Lint + commit**

```bash
cd infra && MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api ruff check app tests scripts
```

```bash
git add backend/app/services/alerting backend/app/workers/tasks.py backend/app/config.py backend/tests/test_alert_channels.py backend/tests/test_alert_webhook.py
git commit -m "refactor(alertes): le canal devient selectionnable (ALERT_CHANNEL)

Le webhook passe dans channels/ ; un contrat commun (envoyer -> bool, CanalIndisponible)
et get_channel() choisissent l implementation. Refactor a comportement constant : le
defaut reste le webhook, les tests existants le prouvent.

Un ALERT_CHANNEL inconnu leve, il ne se rabat jamais en silence sur un canal par defaut :
un canal muet choisi par erreur reproduirait la panne que ce produit combat."
```

---

### Task 2: La colonne `external_ref` — mémoriser le numéro de ticket

**Files:**
- Modify: `backend/app/db/models.py` (classe `Alert`)
- Create: `backend/migrations/versions/0009_alert_external_ref.py`
- Test: `backend/tests/test_alert_external_ref.py`

**Interfaces:**
- Produces: `Alert.external_ref: str | None` — la référence du ticket créé à l'ouverture, relue à la fermeture pour l'annoter.

- [ ] **Step 1: Écrire le test (échoue)**

Créer `backend/tests/test_alert_external_ref.py` :

```python
"""La colonne qui relie une alerte au ticket ouvert pour elle.

Sans elle, on ne pourrait pas retrouver le ticket a annoter quand l'alerte se ferme.
"""
import uuid
from datetime import datetime, timezone

import pytest
from sqlalchemy import text

from app.db.models import Alert, Tenant
from app.db.session import get_session, tenant_scoped_session


@pytest.fixture
def tenant():
    with get_session() as db:
        t = Tenant(domain=f"ext-{uuid.uuid4().hex[:8]}.test", name="Ext")
        db.add(t)
        db.commit()
        tid = str(t.id)
    yield tid
    with get_session() as db:
        db.query(Alert).filter_by(tenant_id=tid).delete()
        db.query(Tenant).filter_by(id=tid).delete()
        db.commit()


def test_external_ref_stocke_et_relu(tenant):
    with tenant_scoped_session(tenant_id=tenant) as db:
        a = Alert(tenant_id=tenant, kind="never_reported", dedup_key="", severity="critical",
                  payload={}, opened_at=datetime.now(timezone.utc), external_ref="TCK-4242")
        db.add(a)
        db.flush()
        assert db.get(Alert, a.id).external_ref == "TCK-4242"


def test_external_ref_par_defaut_nul(tenant):
    with tenant_scoped_session(tenant_id=tenant) as db:
        a = Alert(tenant_id=tenant, kind="domain_silent", dedup_key="", severity="critical",
                  payload={}, opened_at=datetime.now(timezone.utc))
        db.add(a)
        db.flush()
        assert db.get(Alert, a.id).external_ref is None


def test_colonne_presente_en_base():
    with get_session() as db:
        cols = db.execute(text(
            "SELECT column_name FROM information_schema.columns "
            "WHERE table_name='alert' AND column_name='external_ref'")).all()
    assert cols, "colonne external_ref absente"
```

- [ ] **Step 2: Lancer — échoue**

Run: `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api pytest tests/test_alert_external_ref.py -q`
Expected: FAIL — `TypeError: 'external_ref' is an invalid keyword argument for Alert`

- [ ] **Step 3: Ajouter la colonne au modèle**

Dans `backend/app/db/models.py`, classe `Alert`, ajouter après `closed_notified_at` (ou à la suite des colonnes existantes) :

```python
    # Référence du ticket ouvert pour cette alerte (canal desk365) : posée à l'ouverture,
    # relue à la fermeture pour ANNOTER le ticket. Nulle si aucun ticket n'a été créé
    # (alerte non critique, ou canal indisponible à l'ouverture).
    external_ref: Mapped[str | None] = mapped_column(Text)
```

- [ ] **Step 4: Écrire la migration**

Créer `backend/migrations/versions/0009_alert_external_ref.py` :

```python
"""Reference externe d'une alerte : le numero du ticket ouvert pour elle (canal desk365).

Posee a l ouverture, relue a la fermeture pour annoter le ticket. Nullable : une alerte
non critique, ou ouverte alors que le helpdesk etait injoignable, n a pas de ticket.

Revision ID: 0009_alert_external_ref
"""
import sqlalchemy as sa
from alembic import op

revision = "0009_alert_external_ref"
down_revision = "0008_alert_notified_split"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column("alert", sa.Column("external_ref", sa.Text(), nullable=True))


def downgrade() -> None:
    op.drop_column("alert", "external_ref")
```

Vérifier que `down_revision` pointe bien sur la dernière migration réelle (`0008_alert_notified_split`).

- [ ] **Step 5: Appliquer**

Run: `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" migrate alembic upgrade head`
Expected: `Running upgrade 0008_alert_notified_split -> 0009_alert_external_ref`

- [ ] **Step 6: Lancer + isolation (non-régression)**

Run: `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api pytest tests/test_alert_external_ref.py tests/test_tenant_isolation.py -q`
Expected: PASS

- [ ] **Step 7: Lint + commit**

```bash
git add backend/app/db/models.py backend/migrations/versions/0009_alert_external_ref.py backend/tests/test_alert_external_ref.py
git commit -m "feat(db): alert.external_ref — relie une alerte au ticket ouvert pour elle

Posee a l ouverture (canal desk365), relue a la fermeture pour annoter le ticket.
Nullable : une alerte non critique, ou ouverte quand le helpdesk etait injoignable, n a
pas de ticket."
```

---

### Task 3: Le canal Desk365 — un ticket à l'ouverture, une note à la fermeture

**Files:**
- Modify: `backend/app/services/alerting/channels/desk365.py` (remplace le stub)
- Modify: `backend/app/config.py` (les 7 clés `desk365_*`)
- Test: `backend/tests/test_desk365.py`

**Interfaces:**
- Consumes: `CanalIndisponible` (Task 1), `Alert.external_ref` (Task 2), les objets `Alert` / `Tenant`.
- Produces: `desk365.envoyer(event, alert, tenant) -> bool` conforme au contrat de canal.

**Ce que le canal fait, et ne fait pas :**
- `opened` + `severity == "critical"` → crée un ticket, pose `alert.external_ref`, renvoie `True`.
- `opened` + non critique → `False` (aucun ticket).
- `closed` + `external_ref` renseigné → ajoute une note, renvoie `True`.
- `closed` + `external_ref` nul → `False`.
- pas de clé API → `False` + journalisé.
- panne API / réponse illisible / pas de numéro de ticket → `CanalIndisponible`.

**Sur le format exact de l'API** : les champs de `tickets/create` sont documentés (`contact_email`, `subject`, `description`, `group`, `priority`, `category`, `sub_category`). Le chemin et les champs de l'action « ajouter une note » ne sont pas 100 % confirmés — c'est pourquoi **tout passe par une seule fonction de transport `_call(method, path, body)`** que les tests moquent : les tests prouvent la LOGIQUE (quand créer / annoter / s'abstenir), et le **test réel de la tâche 4** prouve le FORMAT contre la vraie API, où l'on ajustera au besoin les constantes de chemin.

- [ ] **Step 1: Ajouter la configuration Desk365**

Dans `backend/app/config.py`, à la suite de `alert_channel`, ajouter :

```python
    # --- Canal Desk365 (helpdesk) ---
    # Base d'API v3. La cle est un SECRET : fournie en variable d'environnement, jamais
    # committee. Vide => canal non configure (envoyer renvoie False, journalise).
    desk365_base_url: str = "https://lsi-maintenance.desk365.io/apis/v3"
    desk365_api_key: str = ""
    # Parametres FIXES du ticket. Valeurs par defaut = celles demandees ; modifiables par
    # variable d'environnement sans toucher au code.
    desk365_requester_email: str = "alerte_dmarc@lsiparis.tech"
    desk365_group: str = "Support informatique"
    desk365_priority: int = 20               # 20 = Urgent (bareme Desk365)
    desk365_category: str = "Réseau"
    desk365_subcategory: str = "Déliverabilité emails"
```

- [ ] **Step 2: Écrire les tests (échouent)**

Créer `backend/tests/test_desk365.py` :

```python
"""Le canal Desk365. L'API est moquee via `_call` : ces tests prouvent la LOGIQUE
(quand creer un ticket, quand annoter, quand s'abstenir), pas le format sur le fil (ca,
c'est le test reel de la tache 4).
"""
import uuid
from datetime import datetime, timezone

import pytest

from app.services.alerting.channels import desk365
from app.services.alerting.channels.base import CanalIndisponible


class _Alert:
    def __init__(self, severity="critical", kind="never_reported",
                 external_ref=None, closed_at=None):
        self.id = uuid.uuid4()
        self.kind = kind
        self.severity = severity
        self.dedup_key = ""
        self.payload = {"domain": "client.fr", "age_days": 30}
        self.opened_at = datetime(2026, 7, 15, 8, 0, tzinfo=timezone.utc)
        self.closed_at = closed_at
        self.external_ref = external_ref


class _Tenant:
    id = uuid.uuid4()
    domain = "client.fr"


@pytest.fixture(autouse=True)
def _configure(monkeypatch):
    monkeypatch.setattr(desk365.settings, "desk365_api_key", "cle-de-test")
    monkeypatch.setattr(desk365.settings, "desk365_base_url",
                        "https://lsi-maintenance.desk365.io/apis/v3")


def _mock_call(monkeypatch, resp=None, capture=None):
    def fake(method, path, body):
        if capture is not None:
            capture.append((method, path, body))
        return resp or {}
    monkeypatch.setattr(desk365, "_call", fake)


def test_ouverture_critique_cree_un_ticket_et_pose_external_ref(monkeypatch):
    calls = []
    _mock_call(monkeypatch, resp={"ticket_number": "TCK-777"}, capture=calls)
    a = _Alert(severity="critical")

    envoye = desk365.envoyer("opened", a, _Tenant())

    assert envoye is True
    assert a.external_ref == "TCK-777"
    method, path, body = calls[0]
    assert method == "POST" and "create" in path
    assert body["contact_email"] == "alerte_dmarc@lsiparis.tech"
    assert body["group"] == "Support informatique"
    assert body["priority"] == 20
    assert body["category"] == "Réseau"
    assert body["sub_category"] == "Déliverabilité emails"
    assert "client.fr" in body["subject"]


def test_ouverture_avertissement_ne_cree_rien(monkeypatch):
    calls = []
    _mock_call(monkeypatch, capture=calls)
    a = _Alert(severity="warning")

    envoye = desk365.envoyer("opened", a, _Tenant())

    assert envoye is False
    assert a.external_ref is None
    assert calls == []                      # aucun appel API


def test_fermeture_avec_ticket_ajoute_une_note(monkeypatch):
    calls = []
    _mock_call(monkeypatch, capture=calls)
    a = _Alert(external_ref="TCK-777",
               closed_at=datetime(2026, 7, 15, 9, 0, tzinfo=timezone.utc))

    envoye = desk365.envoyer("closed", a, _Tenant())

    assert envoye is True
    method, path, body = calls[0]
    assert method == "POST" and "note" in path.lower()
    assert "TCK-777" in str(body.values())    # le ticket est reference
    # on annote, on ne cloture pas : aucun changement de statut demande, juste une note
    joint = " ".join(str(v) for v in body.values())
    assert "résolue" in joint and "clôturez" in joint


def test_fermeture_sans_ticket_ne_fait_rien(monkeypatch):
    calls = []
    _mock_call(monkeypatch, capture=calls)
    a = _Alert(external_ref=None,
               closed_at=datetime(2026, 7, 15, 9, 0, tzinfo=timezone.utc))

    envoye = desk365.envoyer("closed", a, _Tenant())

    assert envoye is False
    assert calls == []


def test_non_configure_ne_leve_pas_mais_ne_se_tait_pas(monkeypatch):
    monkeypatch.setattr(desk365.settings, "desk365_api_key", "")

    envoye = desk365.envoyer("opened", _Alert(), _Tenant())

    assert envoye is False                   # rien fait, mais journalise (voir le module)


def test_reponse_sans_numero_de_ticket_leve(monkeypatch):
    """Sans numero exploitable, on ne peut pas suivre le ticket : on retente plutot que
    de poser un external_ref faux."""
    _mock_call(monkeypatch, resp={"message": "created but weird"})
    a = _Alert(severity="critical")

    with pytest.raises(CanalIndisponible):
        desk365.envoyer("opened", a, _Tenant())
    assert a.external_ref is None


def test_panne_api_leve_canal_indisponible(monkeypatch):
    def casse(method, path, body):
        raise CanalIndisponible("500")
    monkeypatch.setattr(desk365, "_call", casse)

    with pytest.raises(CanalIndisponible):
        desk365.envoyer("opened", _Alert(severity="critical"), _Tenant())
```

- [ ] **Step 3: Lancer — échoue**

Run: `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api pytest tests/test_desk365.py -q`
Expected: FAIL — `NotImplementedError` / `AttributeError: _call`

- [ ] **Step 4: Écrire le canal**

Remplacer entièrement `backend/app/services/alerting/channels/desk365.py` par :

```python
"""Canal d'alerte Desk365 : une alerte critique ouvre un ticket, sa fermeture l'annote.

Tout le couplage a Desk365 est ICI. Le reste du systeme d'alertes ne connait que le
contrat `envoyer(event, alert, tenant) -> bool` (voir channels/base.py).

Regles heritees du systeme d'alertes, inchangees :
 - Non configure (pas de cle API) != silencieux : on journalise, on ne cree rien.
 - L'API en panne leve `CanalIndisponible` -> Celery retente. On perd une notification,
   jamais une alerte.

Seules les alertes CRITIQUES ouvrent un ticket : un avertissement (tls_failure en mode
testing, ou rien n'est bloque) reste visible sur la page Alertes sans inonder le support
d'un ticket Urgent. La selection porte sur la SEVERITE, jamais sur le kind -- un futur
detecteur critique obtient un ticket sans code en plus.

On ANNOTE a la fermeture, on ne CLOTURE jamais : une alerte qui se ferme puis rouvre
ballotterait le ticket, et cloturer sans qu'un humain ait verifie est le contraire de ce
qu'un support veut.
"""
from __future__ import annotations

import json
import urllib.error
import urllib.request
from datetime import datetime, timezone

import structlog

from app.config import settings
from app.services.alerting.channels.base import CanalIndisponible

log = structlog.get_logger()

TIMEOUT = 15.0

# Meme voix que la page Alertes (frontend/src/pages/Alerts.tsx) : le technicien lit la
# meme chose dans le ticket et a l'ecran.
_NATURES = {
    "never_reported": "aucun rapport reçu depuis l'ajout du domaine",
    "domain_silent": "les rapports ont cessé d'arriver",
    "tls_failure": "échec de chiffrement TLS",
}


def _sujet(alert, tenant) -> str:
    quoi = _NATURES.get(alert.kind, alert.kind)
    return f"[DMARC] {tenant.domain} — {quoi}"


def _description(alert, tenant) -> str:
    lignes = [
        f"Domaine : {tenant.domain}",
        f"Type d'alerte : {alert.kind} ({alert.severity})",
        "",
        "Ce que la plateforme a constaté :",
    ]
    for cle, valeur in (alert.payload or {}).items():
        lignes.append(f"  - {cle} : {valeur}")
    lignes += ["", "Détail et historique : page Alertes de la plateforme de rapports DMARC."]
    return "\n".join(lignes)


def _call(method: str, path: str, body: dict) -> dict:
    """Un appel à l'API Desk365. Toute erreur réseau/HTTP/illisible -> CanalIndisponible.

    Point d'étranglement UNIQUE : les tests le moquent (ils prouvent la logique), et le
    test réel de bout en bout valide le format sur le fil.
    """
    url = f"{settings.desk365_base_url.rstrip('/')}/{path.lstrip('/')}"
    data = json.dumps(body).encode()
    req = urllib.request.Request(            # noqa: S310 — URL d'exploitation, configurée
        url, data=data, method=method,
        headers={"Content-Type": "application/json",
                 "Authorization": settings.desk365_api_key},
    )
    try:
        with urllib.request.urlopen(req, timeout=TIMEOUT) as r:   # noqa: S310
            raw = r.read().decode("utf-8", "replace")
    except (OSError, urllib.error.URLError) as exc:
        raise CanalIndisponible(f"Desk365 {path} : {exc}") from exc
    try:
        return json.loads(raw) if raw.strip() else {}
    except ValueError as exc:
        raise CanalIndisponible(f"Desk365 {path} : réponse illisible") from exc


def _creer_ticket(alert, tenant) -> str:
    """Crée le ticket, renvoie son numéro. Lève si la réponse n'est pas exploitable."""
    body = {
        "contact_email": settings.desk365_requester_email,
        "subject": _sujet(alert, tenant),
        "description": _description(alert, tenant),
        "group": settings.desk365_group,
        "priority": settings.desk365_priority,
        "category": settings.desk365_category,
        "sub_category": settings.desk365_subcategory,
    }
    resp = _call("POST", "tickets/create", body)
    numero = resp.get("ticket_number") or resp.get("ticket_id") or resp.get("id")
    if not numero:
        # Sans numero, impossible d'annoter le ticket a la fermeture : on retente plutot
        # que de poser un external_ref faux.
        raise CanalIndisponible(f"Desk365 create : pas de numéro de ticket ({resp!r})")
    return str(numero)


def _ajouter_note(ticket_ref: str, alert) -> None:
    quand = (alert.closed_at or datetime.now(timezone.utc)).isoformat()
    _call("POST", "tickets/add_note", {
        "ticket_number": ticket_ref,
        "body": f"Condition résolue le {quand}. Vérifiez puis clôturez ce ticket.",
        "private": True,
    })


def envoyer(event: str, alert, tenant) -> bool:
    if not settings.desk365_api_key or not settings.desk365_base_url:
        log.warning("alerting.desk365_non_configure", alert_event=event,
                    kind=alert.kind, domain=tenant.domain)
        return False

    if event == "opened":
        if alert.severity != "critical":
            return False        # un avertissement ne cree pas de ticket
        alert.external_ref = _creer_ticket(alert, tenant)   # commit par l'appelant
        log.info("alerting.desk365_ticket_cree", ticket=alert.external_ref,
                 kind=alert.kind, domain=tenant.domain)
        return True

    # event == "closed"
    if not alert.external_ref:
        return False            # aucun ticket a annoter
    _ajouter_note(alert.external_ref, alert)
    log.info("alerting.desk365_note_ajoutee", ticket=alert.external_ref,
             domain=tenant.domain)
    return True
```

- [ ] **Step 5: Lancer les tests**

Run: `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api pytest tests/test_desk365.py tests/test_alert_channels.py -q`
Expected: PASS (7 + 4)

- [ ] **Step 6: Suite complète + lint + commit**

```bash
cd infra && MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api sh -c "pytest -q && ruff check app tests scripts"
```

```bash
git add backend/app/services/alerting/channels/desk365.py backend/app/config.py backend/tests/test_desk365.py
git commit -m "feat(alertes): canal Desk365 — ticket a l ouverture, note a la fermeture

Une alerte critique ouvre un ticket (demandeur alerte_dmarc@lsiparis.tech, groupe/
priorite/categorie fixes) et memorise son numero. A la fermeture, on ANNOTE le ticket ;
jamais de cloture automatique. Un avertissement ne cree rien.

Tout passe par une seule fonction de transport _call que les tests moquent : ils prouvent
la logique, le test reel de bout en bout prouvera le format sur le fil."
```

---

### Task 4: Configuration de production, et le test réel de bout en bout

**Files:**
- Modify: `infra/portainer-stack.npm.yml` (service `worker`), `infra/docker-compose.yml` (service `worker`)
- Aucune modification de code.

**Interfaces:**
- Consumes: le canal `desk365` (Task 3), la config (Task 3).

- [ ] **Step 1: Câbler les variables en production**

Dans `infra/portainer-stack.npm.yml`, service `worker`, à la suite des variables `ALERT_*` existantes, ajouter :

```yaml
      # Canal d'alerte : "desk365" cree un ticket helpdesk au lieu de poster un webhook.
      ALERT_CHANNEL: ${ALERT_CHANNEL:-webhook}
      # La cle API Desk365 est un SECRET (variable d'environnement de la stack, jamais
      # committee). Les autres parametres ont des defauts (voir app/config.py).
      DESK365_API_KEY: ${DESK365_API_KEY:-}
      DESK365_BASE_URL: ${DESK365_BASE_URL:-https://lsi-maintenance.desk365.io/apis/v3}
```

Dans `infra/docker-compose.yml`, service `worker`, ajouter les mêmes trois lignes (le dev
n'aura pas de clé, donc le canal restera non configuré et journalisé — comportement voulu).

**Note** : le service `beat` n'a pas besoin de ces variables — il ne fait qu'ordonnancer,
c'est le `worker` qui exécute `notify_alerts`. Ne pas les lui ajouter.

- [ ] **Step 2: Vérifier que le compose reste valide**

Run:
```bash
python -c "import yaml; [yaml.safe_load(open(f)) for f in ['infra/portainer-stack.npm.yml','infra/docker-compose.yml']]; print('YAML valide')"
```
Expected: `YAML valide`

- [ ] **Step 3: Commit**

```bash
git add infra/portainer-stack.npm.yml infra/docker-compose.yml
git commit -m "chore(prod): variables du canal Desk365 sur le worker

ALERT_CHANNEL + DESK365_API_KEY + DESK365_BASE_URL. La cle reste un secret de stack. Le
service beat n en a pas besoin (il ordonnance, il n execute pas notify_alerts)."
```

- [ ] **Step 4: TEST RÉEL DE BOUT EN BOUT — avec une vraie clé API**

⚠️ **Cette étape exige une vraie clé Desk365 et joint réellement l'API.** Elle valide ce
qu'aucun test moqué ne peut : que le ticket **apparaît réellement dans Desk365** avec les
bons paramètres, et que la note s'ajoute à la fermeture. C'est là qu'on confirme (ou
ajuste) les chemins `tickets/create` / `tickets/add_note` et les noms de champs contre la
vraie API. **À exécuter par l'opérateur** (la clé ne doit pas être collée dans un chat) :
fournir la clé via une variable d'environnement du conteneur.

Depuis un shell sur la machine hôte (ou en injectant `DESK365_API_KEY` dans l'environnement
du conteneur `api`), lancer un scénario complet contre la vraie API :

```bash
cd infra && docker compose exec -T -e DESK365_API_KEY="<CLE_REELLE>" -e ALERT_CHANNEL=desk365 api python - <<'PY'
import types
from app.services.alerting.channels import desk365

# Fausses entites minimales — on n'ecrit PAS en base, on teste juste l'appel reel.
alert = types.SimpleNamespace(
    id="test", kind="never_reported", severity="critical", dedup_key="",
    payload={"domain": "exemple-test.fr", "age_days": 30},
    opened_at=None, closed_at=None, external_ref=None)
tenant = types.SimpleNamespace(id="test", domain="exemple-test.fr")

print("Creation du ticket...")
envoye = desk365.envoyer("opened", alert, tenant)
print("  envoye :", envoye, "| ticket :", alert.external_ref)

import datetime
alert.closed_at = datetime.datetime.now(datetime.timezone.utc)
print("Ajout de la note de fermeture...")
print("  envoye :", desk365.envoyer("closed", alert, tenant))
PY
```

Attendu :
- « Creation du ticket... envoye : True | ticket : <un vrai numero> » ;
- « Ajout de la note... envoye : True » ;
- **et surtout**, dans l'interface Desk365 : un ticket **Urgent**, groupe **Support
  informatique**, catégorie **Réseau**, sous-catégorie **Déliverabilité emails**,
  demandeur **alerte_dmarc@lsiparis.tech**, avec la note de résolution.

Si un appel lève `CanalIndisponible` (mauvais chemin, champ inattendu, ou le contact
demandeur doit préexister), lire le message, ajuster les constantes de chemin/champ dans
`channels/desk365.py`, et rejouer. Ne pas considérer la tâche finie tant qu'un vrai ticket
n'est pas apparu avec la bonne note.

Supprimer le ticket de test dans Desk365 une fois la vérification faite.

## Vérification finale

- [ ] `MSYS_NO_PATHCONV=1 docker compose run --rm --no-deps -v "D:/code/dmarc/backend:/app" api pytest -q` — suite verte
- [ ] `… pytest tests/test_tenant_isolation.py -q` — **bloquant**
- [ ] `… ruff check app tests scripts` — `All checks passed!`
- [ ] Test réel : un ticket Desk365 apparaît avec les bons paramètres, la note s'ajoute à la fermeture.

## Ce que ce plan ne fait PAS, délibérément

- **Clôturer le ticket automatiquement.** On annote ; l'humain vérifie et clôture.
- **Diffuser sur webhook ET Desk365 en même temps.** Un seul canal actif. L'abstraction le permettrait plus tard sans refonte — YAGNI aujourd'hui.
- **Mapper la sévérité vers la priorité Desk365.** Seules les critiques créent un ticket, toutes Urgent.
- **Remonter l'état du ticket vers la plateforme.** La plateforme émet, Desk365 gère la vie du ticket.
