# ============================================================================
# Stack PORTAINER — images pré-construites depuis GHCR (aucun build sur l'hôte).
#
# Déploiement (mode Repository, comme portainer-stack.yml) :
#   Repository URL : https://github.com/LSIParis/report-ingestion-platform
#   Compose path   : infra/portainer-stack.ghcr.yml
#
# Prérequis :
#  - Les images sont poussées par le workflow .github/workflows/build-images.yml
#    (ghcr.io/lsiparis/report-api et report-frontend).
#  - Rendre ces packages PUBLICS sur GitHub (Packages → chaque image → Package
#    settings → Change visibility → Public), OU ajouter des identifiants de
#    registre GHCR dans Portainer (Registries) pour un pull authentifié.
#
# Portainer clone le repo → les fichiers Caddyfile / init-roles.sql sont présents
# pour les montages. Les images sont TIRÉES (pas construites) : déploiement rapide.
# ============================================================================
services:
  caddy:
    image: caddy:2
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN}
      FILES_DOMAIN: ${FILES_DOMAIN}
      ACME_EMAIL: ${ACME_EMAIL}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on: [api, frontend, minio]

  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: reports
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init-roles.sql:/docker-entrypoint-initdb.d/init-roles.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      retries: 5

  minio:
    image: minio/minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - miniodata:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      retries: 5

  createbuckets:
    image: minio/mc
    depends_on:
      minio: { condition: service_healthy }
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD};
      mc mb -p local/reports-raw;
      exit 0;"

  redis:
    image: redis:7
    restart: unless-stopped
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      retries: 5

  clamav:
    image: clamav/clamav:latest
    restart: unless-stopped
    volumes:
      - clamavdb:/var/lib/clamav
    healthcheck:
      test: ["CMD", "clamdcheck.sh"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 180s

  migrate:
    image: ghcr.io/lsiparis/report-api:latest
    # Force le re-tir de l'image a chaque redeploiement : sinon le webhook reutilise
    # l'image :latest locale (perimee) -- voir portainer-stack.npm.yml pour le detail.
    pull_policy: always
    command: alembic upgrade head
    depends_on:
      postgres: { condition: service_healthy }
    environment:
      DATABASE_URL_MIGRATE: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/reports

  api:
    image: ghcr.io/lsiparis/report-api:latest
    pull_policy: always
    restart: unless-stopped
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    depends_on:
      migrate: { condition: service_completed_successfully }
      redis: { condition: service_healthy }
      minio: { condition: service_healthy }
    environment:
      DATABASE_URL_API: postgresql://app_api:app_api@postgres:5432/reports
      DATABASE_URL_WORKER: postgresql://app_worker:app_worker@postgres:5432/reports
      REDIS_URL: redis://redis:6379/0
      S3_ENDPOINT: http://minio:9000
      S3_PUBLIC_ENDPOINT: https://${FILES_DOMAIN}
      S3_BUCKET: reports-raw
      S3_ACCESS_KEY: ${MINIO_ROOT_USER}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY}
      JWT_PRIVATE_KEY: ${JWT_PRIVATE_KEY}
      SENTRY_DSN: ${SENTRY_DSN:-}

  worker:
    image: ghcr.io/lsiparis/report-api:latest
    pull_policy: always
    restart: unless-stopped
    command: celery -A app.celery_app worker -l info --concurrency 4
    # L'image report-api porte un HEALTHCHECK HTTP (GET :8000/health) pense pour l'API.
    # Ce service ne sert aucun port : sans ce disable, Docker le marque unhealthy en
    # boucle alors qu'il tourne parfaitement -- un faux negatif qui masque les vrais.
    healthcheck: { disable: true }
    depends_on:
      migrate: { condition: service_completed_successfully }
      redis: { condition: service_healthy }
      minio: { condition: service_healthy }
      clamav: { condition: service_started }
    environment:
      DATABASE_URL_API: postgresql://app_api:app_api@postgres:5432/reports
      DATABASE_URL_WORKER: postgresql://app_worker:app_worker@postgres:5432/reports
      REDIS_URL: redis://redis:6379/0
      S3_ENDPOINT: http://minio:9000
      S3_PUBLIC_ENDPOINT: https://${FILES_DOMAIN}
      S3_BUCKET: reports-raw
      S3_ACCESS_KEY: ${MINIO_ROOT_USER}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      ANTIVIRUS_ENABLED: "true"
      CLAMAV_HOST: clamav
      CLAMAV_PORT: "3310"

  # Ordonnanceur des alertes. INDISPENSABLE : un domaine silencieux (ou qui n'a jamais
  # rapporte) ne produit AUCUN evenement -- sans ce service, `domain_silent` et
  # `never_reported` ne se leveraient jamais (seules les alertes TLS, declenchees a
  # l'ingestion, fonctionneraient). Beat n'enfile que des taches (broker Redis) ; le
  # travail avec acces DB tourne sur le worker.
  beat:
    image: ghcr.io/lsiparis/report-api:latest
    pull_policy: always
    restart: unless-stopped
    # `/app` appartient a root dans l'image (COPY avant USER appuser) : le conteneur tourne
    # en appuser, qui ne peut y ecrire le fichier de planification. `/tmp` est le seul
    # repertoire inscriptible -- Beat n'a besoin d'aucune persistance (le planning est
    # declare dans le code, pas lu depuis ce fichier).
    command: celery -A app.celery_app beat -l info -s /tmp/celerybeat-schedule
    healthcheck: { disable: true }      # ordonnanceur, aucun port : voir le worker
    depends_on:
      migrate: { condition: service_completed_successfully }
      redis: { condition: service_healthy }
    environment:
      REDIS_URL: redis://redis:6379/0

  imap-worker:
    image: ghcr.io/lsiparis/report-api:latest
    pull_policy: always
    restart: unless-stopped
    command: python -m app.workers.imap_worker
    healthcheck: { disable: true }      # releve IMAP, aucun port : voir le worker
    depends_on:
      migrate: { condition: service_completed_successfully }
      minio: { condition: service_healthy }
    environment:
      DATABASE_URL_API: postgresql://app_api:app_api@postgres:5432/reports
      DATABASE_URL_WORKER: postgresql://app_worker:app_worker@postgres:5432/reports
      REDIS_URL: redis://redis:6379/0
      S3_ENDPOINT: http://minio:9000
      S3_BUCKET: reports-raw
      S3_ACCESS_KEY: ${MINIO_ROOT_USER}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      IMAP_HOST: ${IMAP_HOST}
      IMAP_USER: ${IMAP_USER}
      IMAP_PASSWORD: ${IMAP_PASSWORD}

  frontend:
    image: ghcr.io/lsiparis/report-frontend:latest
    pull_policy: always
    restart: unless-stopped

volumes:
  pgdata:
  miniodata:
  redisdata:
  clamavdb:
  caddy_data:
  caddy_config:
